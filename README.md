# ImageQueue

Desktop application for batch image generation across multiple AI backends (OpenAI, Google Imagen, FLUX, Draw Things CLI).

## Development

```bash
npm install
npm run dev
```

## Tech Stack

- Electron + TypeScript + React
- Vite (via electron-vite) for bundling